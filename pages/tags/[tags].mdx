---
type: tag
title: Tagged Posts
---

import { useRouter } from 'next/router';

export const TagName = () => {
  const { tags } = useRouter().query;
  console.log(useRouter())
  return tags || null;
};

# Posts Tagged with “<TagName/>”
